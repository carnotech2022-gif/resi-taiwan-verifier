import streamlit as st
import json
import re
import requests
import os
from google import genai
from google.genai import types

st.set_page_config(page_title="Pencocok Resi Taiwan", layout="wide")
st.title("📦 Verifikasi & Pencocokan Alamat Resi Taiwan")
st.caption("Unggah foto resi Taiwan untuk mengekstrak data dan memvalidasi keabsahan alamat.")

# Ambil API key otomatis dari server secrets atau environment variable
api_key = st.secrets.get("GEMINI_API_KEY") or os.environ.get("GEMINI_API_KEY")

# Jika di server belum disetel, sediakan opsi input manual di sidebar sebagai cadangan
if not api_key:
    api_key = st.sidebar.text_input("Gemini API Key:", type="password")

# --- (Sisa fungsi normalize_taiwan_address, validate_taiwan_address, dan UI sama seperti sebelumnya) ---